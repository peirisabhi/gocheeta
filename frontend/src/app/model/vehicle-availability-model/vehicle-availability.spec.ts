import { VehicleAvailability } from './vehicle-availability';

describe('VehicleAvailability', () => {
  it('should create an instance', () => {
    expect(new VehicleAvailability()).toBeTruthy();
  });
});
